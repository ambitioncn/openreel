# M-01 vision 两次私有验证授权包

状态：等待主人授权，当前不允许执行。

建议授权原文：

> 授权 M-01 vision 两次私有验证：embedding-vision，image-to-text 1 次、video-to-text 1 次，零重试，总费用上限 4 元，不部署、不公开发布。

范围严格限定为北京隔离私有 staging、`embedding-vision`、两次提交。第一项使用一张非敏感受控图片，第二项使用一个非敏感短视频；任何失败立即停止，不重试。

按当前已审阅价格基线，每次最大 reservation 为 USD 0.2778，两次合计 USD 0.5556；主人策略上限为人民币 4 元。执行前若模型、路由、价格版本、凭证引用或请求契约发生变化，必须停止并重新审阅。

授权不包含凭证变更、购买或订阅、生产部署、服务控制、公开流量或发布，也不包含任何第三次调用。
